import { Panel } from "../../components/ui";

export default function CasesPage() {
  return (
    <Panel title="Moderation Cases">
      <p>Case timeline with evidence references and final decision states.</p>
    </Panel>
  );
}
