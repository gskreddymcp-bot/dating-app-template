import { Card } from "../../components/card";

export default function ProfilePage() {
  return (
    <Card title="Profile">
      <p>Bio, prompts, photos, preference editing backed by `users_profile`, `photos`, and `preferences`.</p>
    </Card>
  );
}
