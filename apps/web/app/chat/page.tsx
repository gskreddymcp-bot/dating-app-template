import { Card } from "../../components/card";

export default function ChatPage() {
  return (
    <Card title="Chat + Safety">
      <p>Messages enforce throttling and flagged keyword checks before insert.</p>
    </Card>
  );
}
