import { Card } from "../../components/card";

export default function DiscoverPage() {
  return (
    <Card title="Discovery Feed">
      <p>Apply filters by age range, distance, and interests. Feed powered by secure RPCs and RLS constraints.</p>
    </Card>
  );
}
